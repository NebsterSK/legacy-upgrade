# Loaded by `next dev` only (NODE_ENV=development), never by `next build`.
# This is the whole point: .env.local would also apply to production builds and would
# ship canonical/og:url pointing at the local domain.
NEXT_PUBLIC_SITE_URL=https://legacy-upgrade.test
